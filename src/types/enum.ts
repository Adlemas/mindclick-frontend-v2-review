/* eslint-disable no-shadow,no-unused-vars */

export enum Role {
  STUDENT = "STUDENT",
  TEACHER = "TEACHER",
  CENTRE = "CENTRE",
}

export enum Simulator {
  MENTAL = "mental",
  MULTIPLY = "multiply",
  FLASHCARDS = "flashcards",
  TABLES = "tables",
  DIVIDE = "divide",
}
